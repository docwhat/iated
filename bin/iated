#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'
Bundler.setup(:default)
require 'pathname'
$: << (Pathname.new(__FILE__).dirname.dirname + 'lib').to_s

require 'iated/application'

Iated::Application.new.run

# EOF
