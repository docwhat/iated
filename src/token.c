
/**
 * Returns 0 if the token is a valid, known token.
 */
int
token_is_valid(char *token, int size)
{
    return(0);
}

/**
 * Returns a token string.
 */
char *
generate_token()
{
    return "NARF";
}
